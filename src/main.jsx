import React from 'react';
import ReactDOM from 'react-dom/client';
import { GidExpertApp } from './GidExpertApp.jsx';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GidExpertApp />
  </React.StrictMode>,
);
